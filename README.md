# dotfiles

Managed with [GNU Stow](https://www.gnu.org/software/stow/).

## Setup

```bash
cd ~/Workspace/dotfiles
stow -t ~ claude tmux
```

## Packages

- **claude** - Claude Code config (`CLAUDE.md`, `statusline.sh`)
- **tmux** - tmux config (mouse, prefix C-a, pane splits)
